/**
 * Chart REST backend ({@code com.task.chart}).
 *
 * <p>Folder map (every Java source under this package):
 * see {@code backend/src/main/java/README.md}.
 *
 * <p>Layering: {@code controller} → {@code service} / {@code service.impl} →
 * {@code repository} or {@code cache}. HTTP JSON uses {@code dto.request} /
 * {@code dto.response}. JWT lives in {@code security}.
 */
package com.task.chart;
